import { ISwaggerDefinition, ISwaggerBaseDefinition } from '../interfaces';
export declare const baseDefinition: ({ title, description, host, basePath, contact, version, apiVersion, }: ISwaggerBaseDefinition) => ISwaggerDefinition;
